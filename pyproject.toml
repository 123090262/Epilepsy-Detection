[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "epilepsy-detection"
version = "0.1.0"
description = "EEG epilepsy detection project based on CHB-MIT style CSV segments."
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "torch",
    "numpy>=1.24,<2",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "mne",
    "scipy",
    "tqdm",
    "pyyaml",
]

[tool.setuptools.packages.find]
where = ["src"]
