data:
  data_dir: data/CHBMITnew5
  sample_rate: 256
  segment_duration: 2.0
  num_channels: 22
  test_patients:
    - chb06

train:
  batch_size: 32
  num_epochs: 60
  learning_rate: 0.001
  weight_decay: 0.000001
  val_size: 0.1
  random_state: 42

model:
  feature_dim: 128
  hidden_dim: 256
  num_classes: 2

output:
  run_dir: runs
  checkpoint_dir: checkpoints
